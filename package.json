{
  "name": "game-workshop",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "rename": "node rename.js",
    "deploy": "npm run build && rm -fr docs && mkdir docs && cp -R ./dist/* ./docs/ && git add -A && git commit -m 'deploy to github pages' && git push github master"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.8.3",
    "phaser": "^3.55.2"
  },
  "devDependencies": {
    "parcel-reporter-static-files-copy": "^1.5.0"
  }
}
